# 更新日志

本项目所有值得注意的变更都记录在这个文件里。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)（`主版本.次版本.修订号`：不兼容改动 / 新功能 / 修 bug）。

维护约定：
- 平时改代码，把变更随手记到 `[Unreleased]` 段
- 发版时把 `[Unreleased]` 改成版本号 + 日期，开新的空 `[Unreleased]`
- GitHub Release 的发布说明直接复制对应版本的段落

## [Unreleased]

## [2.14.0] - 2026-07-18

### Added
- **终端关闭要确认了**（用户点的：✕ 一下会话就没了，误触代价太高）：点终端标签的 ✕ 先弹确认框——里面的 agent 正在运行时文案加重（「正在运行，确定关闭？」）；复用全局统一的确认弹窗组件，Enter 确定 / Esc 取消；确认文案六语词典同步。

### Changed
- **Agent 用量面板排版重做**（用户嫌丑，方案 B「并线紧凑」定稿）：
  - 汉字标签/说明回 UI 字体（PingFang）——mono 排 CJK 字距发散是丑的主因；全局 Maple 强制是 `!important`，用 `#usage-body` ID 特异性抢回，数字读数仍走 mono；
  - 重置时间并入标签行，每表 3 行变 2 行，一行读完「哪个窗口 · 何时重置 → 用了多少」；% 与数字同基线不再上标；侧栏拖窄时时间省略号截断、绝不折行；
  - 频道名 15.5px/700 配平大名牌，名牌 `margin-left:auto` 右置舞台位。
- **订阅名牌换「鎏光名牌」**（1.5× 立体亮面胶囊，三档三动效）：倍率档（Max 20× / Pro 20× / Pro 5× 等含 × 的一律）**彩虹连续流转** + 暖晕/淡紫双层眩光 + 3.4s 轻浮动；无倍率订阅（Plus / Claude Pro / Max / Team / Enterprise）**鎏金斜光扫过**（浮动与彩虹错拍）；Free 素灰静止。`prefers-reduced-motion` 下动效全停。
- **Codex 窗口标签跟数据走，默认「周配额」**（用户点的：Codex 现在就是周配额，别再显示 5 小时窗口）：按接口返回的 `window_minutes` 判断，不足 2 天才叫「5h 窗口」，否则（含拿不到时长）一律「周配额」——不再写死。
- **「Fable 周」补全为「Fable 周配额」**（用户点的：少了俩字）：模型专属窗口的「周」后缀改为完整的「周配额」，六语词典补上对应规则（此前该行在外语界面根本不翻译）。

### Removed
- **用量面板顶部「⚠ 用量接近上限」红色警告条 + 配套桌面通知**（用户裁定：≥85% 刻度整条翻红已经够醒目）。通知只在面板展开刷新时才可能弹，而面板展开时红条已可见，纯冗余。
- **Codex 行的「● 实时」小标**（用户裁定没用）。

### Fixed
- **终端里冒出格格不入的粗灰滚动条**（用户报的：不知道怎么触发的）：xterm 6 弃用原生滚动条、自绘 VS Code 式方角滑块，往回滚终端历史就会浮现，主题不给 `scrollbarSlider*` 色值时用「前景色 20% 透明」的默认灰。现把它整成和全局滚动条同款：5px 细条、圆角、贴右、跟皮肤强调色（内联样式用 `!important` 压过）。

## [2.13.2] - 2026-07-17

### Fixed
- **主窗拖拽间歇性锁死**（用户报的：拖拽时灵时不灵，得点一下别的窗口才能拖一回，松手又锁）。查出**主根因是自家状态泄漏**：在终端里点开 md/html 路径时，`openTermPath` 习惯性 `setPreviewMax(true)` 全屏内嵌预览——但预览早被 pv-patch 改道去独立小窗了，内嵌预览根本没开，`preview-maxed` 却挂上了 `<html>`，CSS 里 `.desktop.preview-maxed #app { app-region: no-drag }` 随即把主窗拖拽区**整个静默关死**、界面毫无征兆（点 Claude 打印的 md 路径 → 弹小窗 → 主窗拖不动，正是这条链）。修三层（全部真环境端到端验证过）：
  - **源头**（`pv-patch.js`）：改道独立窗后 800ms 内紧跟的 `setPreviewMax(true)` 一律吞掉，内嵌流程不受影响；
  - **自愈**（`drag-patch.js`）：窗口每次聚焦检查「`preview-maxed` 挂着但内嵌预览根本没显示」的僵尸态，发现即摘；
  - **兜底**（`drag-patch.js` 新增 + `main.js` 的 `win:drag` 通道）：针对 Electron 上游另一处 macOS 拖拽区缓存间歇失灵——聚焦时轻推拖拽区逼 Blink 重发；按住拖拽条后窗口若没跟手（原生拖拽没接管），渲染层立刻接手经 IPC 手动移窗。无论底层什么状态，拖拽永远可用；
- **终端里点含空格的路径报「没找到」**（用户报的：`~/Library/Application Support/fanbox/shots/直通截图-….png` 这类路径，Claude 明明说文件就在那，点了却说找不到）：路径识别按空格切 token，这类路径被切成两半——前半段有服务端「空格扩展」验证兜着，但用户点的常是后半段（文件名段），走裸文件名搜索：项目根下搜不到、Spotlight 又不索引 `~/Library`，于是报没找到。修复：服务端 `statWithTail` 验证命中时返回吃掉的字符数（`used`），前端据此把**整条含空格的路径连成一条下划线链接**，点哪一段都按完整路径直开（已 curl 实测：`used:46` 恰好吃到 `.png` 为止，中文散文不误报）；
- **预览小窗切走就找不到**（用户报的：小窗忘了关、切去别的 app 再回来，它被大主窗整个盖住，只能靠调度中心翻）：⌘Tab / 点 Dock 切回 Rurutia 时（`app.on('activate')`），主窗聚焦后把预览窗 `moveTop()` 提到最前——小窗露脸但不抢键盘焦点。它不是独立 app 进不了 ⌘Tab 列表，这是同一 app 内能做到的最接近形态。

### Changed
- **客户端瘦身 ~58MB**：`package.json` 加 `build.files` 白名单，只打包 `electron/ + public/ + server.js` 和生产依赖。之前没有白名单，整个仓库都被塞进 app：32MB 的 `rurutia.patch`、README 宣传图、`docs/` 截图、`experiments/`，starship 还因 extraResources 存了双份，运行时不用的 `@xterm` 源码也在。app.asar 105MB → 46MB，.app 350MB → 292MB，DMG 169MB → 129MB。剩下的大头是 Electron 框架 231MB（固定成本）+ Maple Mono CN 四字重 22MB（界面/终端字体，值得留）+ Monaco 编辑器 13MB。

### Docs
- 新增 **`docs/模块指引.md`**——bug 排查地图：症状 → 第一落点文件的速查表（拖拽/路径点击/皮肤/字体/观察舱/截图直通/用量……）+ 全模块一句话职责 + 查案惯例（分叉文件要 `grep -a`、web/桌面判别、验证套路）。`RURUTIA-PATCH.md` 顶部已挂链接，A/B 表同步补齐 prompt-patch / starship-config / drag-patch 与本轮改动。

## [2.13.1] - 2026-07-17

### Fixed
- **观察舱彩虹来回横跳 bug**（用户报的：闲置卡刚显示彩虹，一点它/一切换 tab 就打回原形，安静一会儿又自己变回彩虹，循环往复）：根因是点击/切换 tab 时终端整屏重绘一次，这股输出爆发被回合机当成「新一轮工作」，安静 6.5s 后又被误判「收工」再放一遍庆典。修复（`public/observer-patch.js`）：收工判定加**活跃秒**门槛——本轮输出至少覆盖 5 个不同的秒才算真工作；重绘爆发只占 1-2 秒，够不着，也打扰不了已收工的卡。

### Changed
- **收工从 8.5s 庆典改为常驻彩虹**：庆典（闪光 + 旋转流光）谢幕后不再打回原格子色，转入 rest 态——静态彩虹渐变边框 + 对角彩虹格慢速流动（hue-rotate 合成器）+ 满格彩虹进度丝 + 白点「已收工」，直到下一轮真实工作（新活跃 ≥3 秒）开始才复原。rest 态存在活动记录上，扛得住模块重建（换绑/开关终端）；
- **格子改彩纸轮转**（用户钦点「五颜六色地不停跳动」）：红=报错、黄=警告保留语义，其余输出不再全归蓝色（旧版蓝海淹屏），改为蓝/绿/强调/紫外四色随机；新增第六格色「紫外」（`--rb-uv` 专色）。同时撤掉「进度翻绿」——不再把整卡刷成纯绿（用户看到的 Revolution 全绿就是它），本轮进度只看卡顶进度丝；
- **面板改悬浮圆角卡**：不再直角贴边，改 12px 边距 + 18px 圆角 + 发丝边 + 柔光 + 亮一档底，跟 soft-patch 的侧栏/导航/终端同一套卡片语言；`#app` 让位宽度同步 +24px，收起位移带上边距不露边。

## [2.13.0] - 2026-07-17

### Added
- **观察舱色阶天梯升级：四档 + 阈值自定义**（用户看过 HTML 效果图拍板，`public/observer-patch.js`，效果图存 `design-demos/观察舱-色阶自定义-效果图.html`）：
  - 新增第二档**紫外**——固定紫罗兰色（皮肤没有紫色状态色，`--accent` 每套皮肤颜色都不同挂不得；浅色皮肤自动换深一档保证可读）。新默认天梯：点火 <1M 素白 → **蓝移 1M** → **紫外 10M** → **鎏金 100M** → **棱镜 500M**（用户定案：一亿封金、五亿成谱，原 500M 金 / 1B 彩虹作废）；
  - **四个阈值全部可自定义**：底栏新增 ⚙ 打开「色阶档位」小浮层，输入支持 `800K / 10M / 1.5B / 1200000` 写法，递增校验（乱填标红 + 中文提示），实时生效、存本机（`rb_obs_tiers`）。只解析被编辑的那一格——没动过的档位保留精确存值，不会被显示用的舍入标签（1234567 显示 1.23M）悄悄改写；
  - 大数字色阶、进度尺、底栏图例（改 JS 动态渲染）、仓位卡里程边框（新增紫边档）全部走同一套自定义阈值，一处改全联动。

### Fixed
- **跨零点数字不回落**：`feedTotal` 原本只许目标值向上，观察舱开过夜、后端 00:00 归零后，大数字会卡在昨天的总量一整个上午（直到今天的量重新超过它）。现在任何 >1K 的回落（跨零点、后端重算）都直接落位重画到真实值；
- **reduced-motion 漏网**：系统开「减弱动态效果」后，千分位逗号和小数点的渐变流光仍在无限滚动（覆盖规则只写了数字轮），现在数字/逗号/小数点三件套全部静止；
- 档位浮层 z-index 提到 320（原 120 会被同角落的截图卡/更新提示压住）；`?shot` 截图模式同步冻结浮层过渡；图例变长把底栏挤到换行时浮层跟随 ⚙ 重新锚定；档位太小（<1K）时给出明确提示而不是「看不懂」。

## [2.12.2] - 2026-07-17

### Fixed
- **观察舱数字轮悬在半路 bug**（用户截图实锤）：机械滚轮的「进位窗口」（低位快进位时高位跟着转一部分）和简写模式的末位连续轮，把分数位置当成了**静止态**——数字不动时，大数字和「精确」行常年有几位卡在两个数字中间，不上不下，看着像卡死了。修复（`public/observer-patch.js`）：滚动过程保留机械连滚手感不变，但 rAF 追上目标后新增 ~300ms ease-out **落位**，把所有轮子送回整数位再停；首次进场、静止时切换读数模式、面板不可见时收尾，也一律直接整位呈现。三种读数模式 + 精确行全部覆盖。

## [2.12.1] - 2026-07-16

### Fixed
- **观察舱大数字字体被全局规则碾掉 → 巨型白逗号 bug**（用户截图实锤）：`soft-patch.css` 里的 `body * { font-family: 'Maple Mono' … !important }` 全局兜底把大数字的 Chakra Petch 整个覆盖了——数字变成圆体 Maple Mono，千分位逗号在该字体下又大又圆、颜色还是没吃到渐变的纯白，看起来像一坨巨大的白色异物。两处修复（`public/observer-patch.js`）：
  - 用更高特异性的 `#rb-obs .rb-big * { font-family: var(--rb-num) !important }` 把数字字体**强行**抢回 Chakra Petch（`.ro-subnum` 精确行同治）；
  - 千分位逗号（`.rb-sep b`）加入色阶渐变选择器，与数字/小数点同色流动——白逗号夹在渐变数字中间太跳（深浅两套皮肤都补了）。
- **今日 Token 读数追不上实际消耗**：原链路 = 服务端 10s 缓存 + 前端 12s 轮询 + rAF 再摊 12s 滚过去，最坏落后真实值 ~30 秒。改成前端 5s 轮询 + 服务端 4s 缓存（每次轮询基本拿到新鲜数据，多窗口仍共享一次扫描），滚动追赶也随之收紧到 5s 内。实测增量扫描预热后单次仅 ~5ms，5s 轮询零压力。

### Changed
- **色阶天梯降档（用户拍板：原目标太难够）**：鎏金 1B → **500M**，棱镜 3B → **1B**（蓝移 1M 不变）。大数字色阶、仓位卡里程边框（m1/m2/m3）、底部天梯图例、里程条下一目标全部同步。

## [2.12.0] - 2026-07-16

### Changed
- **观察舱大数字从「倒数模型」全面改版为「今日 Token 里程」**（用户试用后拍板：倒数不切实际，消耗才是真实发生过的事）。只涨不跌的机械滚轮里程表，统计**当天（00:00 起）全机器**的真实 token 消耗，不只 Rurubox 终端里跑的（`public/observer-patch.js` / `server.js`）：
  - 数据源：Claude Code 读 `~/.claude/projects/**.jsonl` 每条消息的 usage（真 token、含 cache）；Codex 读 `~/.codex/sessions/**` rollout 的 `total_token_usage` 快照按天求增量（跨午夜自动迁移基线）——**Codex 客户端与 CLI 写同一份日志，单开客户端也统计得到**。新增 `/api/obs-tokens`（10s 缓存，观察舱 12s 轮询），两次轮询之间用 rAF 把显示值匀速滚向目标，追上即停（零常驻开销）。一律只读「运行本 App 的用户」自己的家目录，谁装谁读谁的；
  - **色阶天梯（今日口径，用户定案）**：点火 <1M 素白 → 蓝移 1M（`--info`）→ 鎏金 1B（`--yellow`）→ 棱镜 3B（五色流光；从 10B 降到 3B——日常量级 ~1B/天，天天见得到彩虹才是好彩虹）。跨阶一次性闪光 + 徽章弹跳，环境极光随色阶换色，十段刻度里程条显示本阶内 log 进度（跨阶清零像新的一圈）；
  - **读数三模式**（`rb_obs_nummode` 持久化，切换器在「精确」行右侧）：简写 = 三位有效数字 + 单位恒定超大；**K = 只除 1000 其余位全滚（默认，蹦得最欢）**；全显 = 一个零不砍、记分牌两行堆叠（高位行 / 末六位行右对齐）。任一模式下方都有 11 位「精确」小滚轮；
  - 数字字体换方盒 Chakra Petch（woff2 打包 `public/vendor/fonts/`，不依赖 CDN）。
- **观察舱视觉「极光版」**：近黑纵深底 + 色阶极光 + 细颗粒噪点 + 色阶渐变发丝分隔线；仓位卡改玻璃面。浅色皮肤照旧停用辉光/噪点（screen 叠加在浅底出色斑的老教训）。设计过程稿在 `design-demos/观察舱-token里程-样例.html`。

### Added
- **仓位里程边框**：每张仓位卡按「该项目今日消耗」换边框——1M 蓝 / 1B 金 / 3B 彩虹渐变（常驻荣誉，与收工的旋转流光临时庆典视觉区分），计数文字同级变色，右上角显示该项目今日 token 数（perCwd 归属）。
- **外部 agent 识别**：Codex 会话 45 秒内活跃、但 cwd 不属于任何打开的终端 → 观察舱自动冒出一张带紫色「外部」章的仓位卡（如单开的 Codex 客户端），实时显示它烧了多少，安静 60 秒自动收走。
- **收工庆典升级**（回合机判据不变：安静 6.5s 收工）：白闪扫过卡面 + 旋转彩虹流光边框（conic 只转 transform）+ 格子对角彩虹（容器 hue-rotate 合成器流动）持续 8.5s 后自动恢复原格子色；每仓新增 2px「本轮进度丝」（EMA 校准的回合进度，收工瞬间走满变彩虹）。子任务完成庆祝保留。
- **数据源 chips**：hero 下方常显 Claude / Codex 各自今日量，一眼看出两路谁在烧。
- **Agent 用量面板：Claude 官方限额新增「模型专属周配额」**（如 Fable 的独立周窗口，当前 API 字段 `limits[].kind=weekly_scoped`）——完全数据驱动：接口给就画在总配额下面（标签直接用官方 `display_name`，新模型自动跟上），接口撤了自动消失（与 Codex 取消周配额后的表现同款）；也纳入 ≥85% 接近上限警告与桌面通知（`server.js` / `public/app.js`）。
- 无头验收参数：`?rbobs=demo&rbtok=N` 预置今日量、`&rbmode=compact|kilo|full` 预置读数模式、`&rbdone=1` 预置收工卡；`?shot` 下观察舱过渡全瞬时（headless virtual-time 会把过渡冻在起点的坑）。

### Removed
- 倒数模型整套（「未完成任务」/「预计剩余」/ 归零 cascade / 白→彩渗入）：被今日里程取代。回合机（EMA 校准）、格子事件分类、巡场高亮、换绑菜单、资源纪律（合成器动画 / 关闭即零成本）全部保留。

## [2.11.0] - 2026-07-13

### Changed
- **观察舱大数字全面改「倒数模型」，且每一次任务都有倒数**（2026-07-12 夜定案的方向今日落地；用户今日补拍板「总量从哪来」的岔口：不是每个活都有 CC 任务清单，只给大项目倒数就没意义 → **总量可以估，归零必须真**）。三层数据源按可信度递补（`public/observer-patch.js`）：
  1. **有 CC 任务清单** → 「未完成任务」真任务数往下减（原逻辑保留，最准）；
  2. **没清单但终端在干活** → 「预计剩余」回合倒数：每回合（开始吐字 → 安静 6.5 秒判收工）按该终端历史产出估一个总量（EMA、按项目目录存 localStorage `rb_obs_ema`，越用越准；首轮默认 1800），真实输出字节驱动往下减（64 字节 = 1 单位），最后 12% 渐近减速吊在个位数不落地，agent 真停下瞬间归零 → 满彩虹 + 庆祝辉光。macOS 拷贝文件式：估总量、真终点。快问快答（< 40 单位 ≈ 2.5KB）不起倒数不庆祝，防噪音；
  3. **全安静** → 0 待命。原「工作量往上滚」模式删除（方向已被用户否）。
  - 彩色渗入两种倒数同一套语言：完成度越高，彩虹从右往左渗入大数字越多。
- **工作态常驻绿底移除**（2026-07-12 拍板 + 用户浅色皮肤截图实锤）：`.ro-hero.busy` 的绿色 bloom、绿字、绿 text-shadow 全部去掉，工作态只留数字轻呼吸；绿色辉光只属于归零庆祝时刻。（`public/observer-patch.js`）

### Added
- **子任务完成 / 单终端收工的模块庆祝**：某个 CC 任务翻成 `completed`、或某终端一回合干完，对应模块卡片一次性绿光脉冲 + 格子对角波浪闪 + 计数弹跳（全一次性动画，播完自动消失，无常驻开销；`prefers-reduced-motion` 跳过）。（`public/observer-patch.js`）
- **格子 × 大数字上下互动（进度翻绿，用户点名：格子不能跟数字各玩各的）**：格子照常按真实输出事件落色，但绿色占比跟着完成度走——大数字每往下减一截，最老的格子就被「确认完成」翻绿（每拍最多一格 = 波浪感），收工时板子自然近全绿，新回合的事件再逐渐覆盖。任务模式按 已完成/总任务，回合模式按估算完成度。这层互动是 GlintGrid 样例原有的叙事，落地时丢了、现在补回。演示同步修正：只有真在干活的窗口才吐事件（等回话/空闲/收工静默全安静），事件配色按真实终端权重（输出多、错误少）。（`public/observer-patch.js`）
- 观察舱演示新增 `?rbobs=demo&win=N` 参数（N=1..6）：模拟窗口数，专用于无头验收各档布局（1 独占 / 2 竖排 / ≥3 双列）；`?rbobs=demo&work=1` 现在演示回合倒数全流程（起估 → 倒数 → 渐近收尾 → 收工庆祝 → 静默 → 下一轮），无头截图已核三态。（`public/observer-patch.js`）

### Fixed
- **浅色皮肤大数字后面的绿色色斑**（用户截图）：bloom 辉光用 `mix-blend-mode: screen` + 模糊渐变，是深色 UI 的语言，浅底上会洗出一块可见矩形 → 浅色皮肤（`html[data-mode="light"]`）整体停用 bloom，庆祝感由彩虹数字 + 格子瀑布承担。（`public/observer-patch.js`）
- **浅色皮肤格子被稀释成粉彩（用户对比截图定案：跟主题融合可以，色彩感不能弱）**：格子底色 66% 透明度蒙在米底上发灰 → 浅色模式改实心糖果 chip（边框纯功能色、底色 90%），深色皮肤保持原样。顺手让分类器补认 Claude Code 的输出记号（`⏺` 动作完成 → 绿、`✽✢✳` 思考转轮帧 → 紫），真实输出不再一面蓝墙。无头截图已核（现代博物皮肤）。（`public/observer-patch.js`）
- **观察舱 2 窗口格子布局 3×8 → 5×8**（用户定案：两个工作窗口时每块应为 8 列 5 行）：`layoutFor(2)` 从 `{cols:8,rows:3}` 改为 `{cols:8,rows:5}`，无头截图已核（竖排两块、每块 8×5）。（`public/observer-patch.js`）

### 已知取舍（回合倒数 v1）
- 回合收工判据是「安静 6.5 秒」：agent 静默跑超长命令（输出全程缓冲）可能被提前判收工庆祝一次，输出恢复后自动开下一轮，不丢数据只多一次庆祝。后续可用前台进程判断（procTick 已有数据）加固。
- 首轮估算未校准时（新项目首次干活）倒数起点是默认值 1800，可能与实际偏差较大；干完一轮即校准（EMA），越用越准。

## [2.10.2] - 2026-07-12

### Fixed
- **Claude Code 用户消息块又成深板砖（v2.9.1 的续修：靶子从 slot 8 挪到 slot 7）**：v2.9.1 按当时的 CC 行为改的是「背景 = brightBlack（SGR `100` / `48;5;8`，slot 8）」，但新版 Claude Code 已改用「背景 = 白（SGR `47` / `48;5;7`，slot 7）+ 黑字（`30`）」画用户消息 / 输入框——旧改写一次都没命中（从本机真实 CC 抓包确认；它真正改到的那个 `100` 其实是 Starship 的 `❯` 提示符，不是 CC 那块）。而浅色皮肤为让「亮白字」在浅底上可见，`buildAnsi` 把 white(slot 7) 反成深青紫（实测截图那块底色 RGB(99,103,129) 正是 white 槽），于是 CC 拿它当底就成了压在暖底上的深板砖，黑字又被 `minimumContrastRatio` 强行提亮。把 SGR 流改写扩到 slot 7（`47` / `48;5;7`，连老的 `100` / `48;5;8` 一并保留）→ 换成按当前终端底推导的柔和面板色。逐 token 单元测试：CC 那块 → 面板真彩，Starship 真彩 / 256 灰 / 前景码 / logo 黑底青底一律不动（含 `48;2;147;…` 这种真彩分量里含「47」子串的陷阱也不误伤）。（`public/themes-patch.js`）

### Added
- **观察舱「正在工作」信号（用户反馈：CC 一干活就该有存在感，别老杵个 0）**：没有 agent 任务可数时，大数字区不再摆死气沉沉的 `0`——标签换「正在工作」，大数字 = 正在吐字的终端数（绿色 + 柔和呼吸 + 绿色辉光），下方一行「⚡ N 个终端 正在工作 · 名字… · live」直接点名谁在忙。每张模块卡在干活时整块发绿光（绿边 + 绿晕）、计数槽亮一颗 ⚡；等你回话黄光、空闲收成小圆点 `·`。判据仍是「该终端近 4 秒有输出」，每秒刷新；有真 agent 任务时照旧显示任务数，两态互不打架。资源纪律不破（持续动效只切 transform/opacity，辉光走预烘焙 opacity，面板一关全部定时器停摆）。（`public/observer-patch.js`）

### 已知
- **终端里带圈数字 ①②③④ 相邻时字形重叠**：非数据错（字符本身正确、可复制），是 macOS 没有等宽字体自带这些字、一律拿约 1.8 格宽的系统备用字形顶替所致（实测 Maple / Menlo / Monaco / SF Mono / JetBrains / Cascadia / Sarasa / Meslo / Noto CJK / Nerd 十款皆宽）。终端按 1 格排（与 Claude Code 自身排版一致），强改 2 格会让该行右侧整体错位、更糟，故按用户定案维持现状。

## [2.10.1] - 2026-07-11

### Changed
- **截图直通与观察舱升级为工具条一等按钮**（用户定案：新功能按钮全部进终端工具条那排、统一走显/隐筛选）：截图直通=取景框图标（左键截图、右键打开快捷键设置），观察舱=右栏面板图标（高亮跟随面板开合），插在 ⋯ 之前、默认常驻；两颗都进「自定义工具条」勾选清单，跟其他按钮一样可显可隐、折叠后自动以 ⋯ 菜单代理行出现。⋯ 菜单不再重复列这两项（保留「截图快捷键… / 自定义工具条…」）。toolbar-patch 改为最后加载（要等截图/观察舱两补丁先就位）。

## [2.10.0] - 2026-07-11

### Added
- **观察舱：右侧终端工作状态面板**（视觉与交互经 GlintGrid 样例多轮定稿，`design-demos/观察舱-glintgrid-样例.html`）：主窗最右一列可开合面板（终端 ⋯ 菜单「观察舱」开，面板 ✕ 关，开合状态记忆；打开时主区平滑让位并自动 refit 终端网格）。三层实时内容——① **大数字**：打开终端对应 Claude Code 会话的未完成任务数（`~/.claude/tasks` ↔ 终端 cwd 经 `~/.claude/projects` 归属），滚轮式数位、字号随位数自适应（6 位内），白字随完成占比从右往左渐变成彩虹，全部归零且确有完成时进入庆祝态（辉光呼吸 + 全场格子瀑布翻绿）；② **模块**：每个终端 tab 一块，数量自适应布局（1 个独占超大 / 2 个竖排 / ≥3 双列），模块头可点名换绑，状态点区分 工作中（输出 4 秒内）/ 等你回话（无输出但前台是 agent）/ 空闲；③ **格子**：终端输出事件实时点亮，按内容分类上色（错误/警告/成功/思考/输出）。**皮肤全接管**：面板底色、边框、文字、格子五色、跑马灯、连大数字的彩虹渐变都由当前皮肤的状态色变量拼成，18 套皮肤各有其观察舱。**资源纪律**：持续动效只走 transform/opacity 合成器路径，辉光为预烘焙渐变 opacity 切换；面板关闭或窗口隐藏时全部定时器停摆、pty 旁听只记时间戳；任务轮询按 mtime 增量重读。（`public/observer-patch.js` 纯新增）
- **截图直通车·热键版（快捷截图直通终端）**：按全局快捷键（默认 ⌘⇧2，可在终端 ⋯ 菜单「截图快捷键…」里自定义，最多两组组合键、录入即生效）→ macOS 原生交互选区（空格切整窗截取、Esc 取消，不用离开当前窗口）→ 截图落盘到应用数据目录（**桌面从此干净**）→ 路径自动插进当前终端喂给 Claude Code / Codex，同时弹现有直通卡保底「标注 / 收进素材」入口。旧图自动清理：启动时 + 每 6 小时删 48 小时前的（CC 发送那一刻已把图读进对话，删原件不影响历史回看）。首次使用需在系统弹窗里授权「屏幕录制」，权限缺失时给一次性通知指引。终端 ⋯ 菜单里也有「截个图」入口，不按键也能截。（`electron/shot-hotkey.js` + `public/shot-patch.js`）
- **独立预览窗口**：点开 md / 代码 / pdf 等可读文件，默认弹一个真·系统窗口（可拖到任意位置、可缩放、⌘W 或 ✕ 关掉、记住位置大小），不再被定死在主窗上方的内嵌面板里。复用同一套页面的「纯预览模式」（`?pv=` 参数）：皮肤自动跟随，md 依旧所见即所得可编辑、自动保存。单窗复用：再点开别的文件就在同一窗里换内容。逃生门：⌥点击 = 临时用相反方式；内嵌预览头部新增 ↗ 按钮（左键弹窗、右键切换默认打开方式）。图片/视频、「文件跟随」实时预览维持内嵌不变。安全阀：预览窗按 web 版零桥跑（pty/微信/录像桥一律不暴露），绝不与主窗抢终端数据路由。（`electron/pv-window.js` + `public/pv-patch.js`）
- **终端工具条折叠**：term-actions 一排 12 颗按钮太挤——现在默认只露「agent 启动键 + 新终端 + ⋯ + ✕」，其余收进 ⋯ 溢出菜单（实时代理行，微信连接小红点折叠后镜像到 ⋯ 上）。⋯ 菜单底部「自定义工具条…」勾选面板：勾谁谁常驻，即改即生效（localStorage 持久化）。agent 启动键与 ✕ 恒常驻。（`public/toolbar-patch.js`）
- **一键启动 agent 收录 Grok CLI**：AGENT_REGISTRY 加 `grok`（xAI 官方 CLI，安装命令 `curl -fsSL https://x.ai/cli/install.sh | bash`），配 X 风格单色图标，勾选面板即可启用。

## [2.9.1] - 2026-07-11

### Fixed
- **Claude Code 用户消息回显成大亮砖（v2.9.0 终端配色的回归）**：CC 在 `dark-ansi` 下把「用户消息背景」绑在 ANSI brightBlack（发 SGR `100`），文字用 white/brightWhite——按「stock 终端 brightBlack=深灰」的假设设计。而 v2 调色板为了框线发光把 brightBlack 调成亮强调色（前景用途），结果整段消息成了高饱和大色块（电光紫下 #9f77c6，对终端底 5:1），浅字还被 xterm 的 `minimumContrastRatio` 强行压黑，十分违和；18 套皮肤全中，浅色 9 套更是深块配深字（2.4–2.8:1 几乎不可读）。槽位一色两用（框线前景 + 消息背景）在 16 色调色板层面无解，改为在数据进 xterm 前做一层 SGR 流改写：只把「当背景用的 slot 8」（`100` / `48;5;8`）换成按当前终端底推导的真彩面板色（暗底提亮一档、浅底压深一档，与皮肤 bg-3 同律，自动含项目淡染/自选背景），前景用途的 brightBlack（`90` / `38;5;8`）一概不动，框线发光保留；CC 自己的浅字在面板上天然可读。改写器逐 token 走 SGR 参数（38;2/38;5 整组跳过，不会把真彩分量里的 100 误当背景码），跨 chunk 截断的转义序列扣尾拼接。已验证：27KB 真实 CC 抓包 10 处全改、其余字节零变动、任意分块切法结果一致；真实浏览器 + xterm 里 `[100m` 落成面板真彩。

## [2.9.0] - 2026-07-11

### Added
- **终端配色自选面板**：嵌在侧栏「皮肤」块内（紧贴皮肤选择器，带一行说明「默认已跟皮肤调好，想自定义再改」；不再被后加载的提示符/语言选择器挤到底部）。20 个槽位全部按 Claude Code 里的实际用途标注（框线/暗淡字＝对话框边框、蓝＝信息横幅、青＝链接路径、绿＝成功新增、红＝错误删除、黄＝警告等待……），点色块即改、所有开着的终端（含正跑着的 Claude Code / Codex）即时变色；每套皮肤分开记忆（localStorage `fb_term_colors`），可逐项 ↺ 或整套还原。自选了背景时 tintTheme 不再抢着盖回皮肤底色。取色不走系统原生弹窗，自绘取色器（SV 面 + 色相条 + hex 输入 + 新旧对照块）与皮肤同一套面板变量，拖动即时生效。
- **预览工具**：`design-demos/gen-terminal-preview.js` 从 themes-patch 真代码取色，生成 18 套皮肤旧 vs 新的 Claude Code / Codex 界面模拟对照页（terminal-colors-preview.html，待用户过目定稿）。

### Changed
- **浅色 9 套整体降亮度（Vela 契约 v3，用户反馈长时间盯着刺眼）**：页面底压进 52–62% 亮度带（原 67–80%；>62% 按 4% 一档下压最多五档，酸性时尚本就在带内不动），内容面 `lighten` 0.26→0.10、面板 0.14→0.05，最亮表面从 75–85% 降到 61–64%；边框 0.12→0.14 加固骨线，暗淡字混合 0.40→0.32（全部抬回 3:1 以上），Monaco 沟槽 0.4→0.12。色相不动，皮肤还是那张脸。终端 ANSI 可读性目标对新底重算（`buildAnsi` 收生效底色参数），浅色彩字最低对比 3.22:1，正文 6.0–10.9:1 全过 AA。实测（浏览器 computed style）：现代博物 body #EFE6D2(80%)→#d3ccba(61%)。
- **终端 ANSI 16 色 v2——真跟皮肤 + 醒目**（修「换肤终端没变化」的根因：18 套皮肤原共用一副 Catppuccin 锚色、只偏皮肤色 10%，dark-ansi 下的 Claude Code 换肤几乎无感）：蓝/品红/青三个关系槽按色相就近把皮肤三强调色请上台（像素光的品红槽就是那支 #FF3D8B）；红/绿/黄语义槽保色相拉饱和；brightBlack（Claude Code 对话框框线/思考灰字）往皮肤主强调色偏 40%，框线跟皮肤发光。可读性全量整备：浅底彩字对终端底对比 ≥3.2、暗底 ≥2.8。Codex 同一副调色板，自动跟随。
- **Claude 启动命令按皮肤明暗现选主题**：浅皮肤 `light-ansi`、深皮肤 `dark-ansi`（点击时求值，换肤后再启动就跟上）。

### Merged
- **上游 FanBox v2.4.0 → v2.6.2 全量合入**（基线 v2.3.3 → v2.6.2，+1817 行）：
  - 11 个 coding agent 一键启动注册表 + ⚙ 设置面板（勾选启停、未装点击复制安装命令、WebGL 兼容渲染开关）；工具栏保留 Rurutia 内联 SVG 图标，claude/codex 按钮改上游动态注入（id 不变、CSS 依旧命中），普通终端按钮保留。
  - 回合安全带：agent 每轮开工前自动存档 + 一键回滚 + 影子 git 兜底非 git 项目（状态栏新增「回合存档」）。
  - xterm 6.0 全家：中文乱码根治（CapsLock IME 补丁已入上游 6.0 正式版，按退场条件删除 `check:vendor-patch` 守卫）+ 图集自动保养（v2.6.0/2.6.1）+ 压力监视真重建（v2.6.2）。
  - 更新胶囊一键下载 dmg、Intel x64 构建脚本（公证 profile 换 rurutia-notary，不收上游作者签名证书）、窄布局文件名优先 (#49)。
  - i18n：上游 26 条新词条 + 8 条插值规则补齐 ja/ko/fr/es/zh-TW 五语言（英文词典上游自带）。「终端配色」面板文案暂为中文，待定稿后补翻。

## [2.8.1] - 2026-06-29

### Fixed
- **应用内语言选择器点开「没反应」**：v2.8.0 新增的侧栏「语言」选择器，容器 `#lang-switch` 漏加了 `position:relative`，而下拉面板 `.fbx-skin-pop` 是 `position:absolute`——于是下拉以 `<body>` 为定位基准、按 `bottom:calc(100% + 6px)` 被顶到视口上方（实测 `top:-255`）整条飞出屏幕，点开后看不见，表现为「点了没反应」。皮肤 / 提示符两个同款选择器各自都有这条规则，唯独新加的语言选择器漏了。给 `#lang-switch` 补上 `position:relative` 后，下拉正常浮在选择器正上方。

## [2.8.0] - 2026-06-29

### Added
- **应用内多语言：从「中/英二选一」扩成 7 种界面语言**（简体中文 / 繁體中文 / English / 日本語 / 한국어 / Français / Español），与 GitHub 主页支持的语言一致。侧栏「皮肤 / 提示符」下面新增「语言」选择器，点开是 7 种语言的母语名列表，选一个即切换（写 `localStorage` + `config.json`，刷新生效）。`i18n.js` 从二元开关重写成 N 语言加载器（中文为源语言，其余各有一份 `i18n-dict.<lang>.js`：365 条词条 + 68 条插值规则，全部按各语言语法本地化）；用户内容区（预览 / 编辑器 / 终端）一律不翻译。
- **终端复制粘贴**（合并上游 FanBox #27）：引入 `@xterm/addon-clipboard`；⌘C 有选中文本则复制、无选中发 SIGINT（对齐原生终端），⌘V 粘贴，右键菜单复制 / 粘贴，选中即复制（iTerm2 习惯），⌘+/⌘-/⌘0 字号缩放（10–24px）；焦点在终端时不再被全局键盘逻辑拦截方向键。
- **项目内新增 skill 即时可见**（合并上游 FanBox #28）：`skillsData/agentProjects` 支持跳过缓存，新增 `POST /api/skills/refresh`（可追加扫描当前浏览目录）；打开「Skills 透视」即强制刷新并带上当前目录，即使该项目不在「最近 12 个活跃项目」里也能扫到它的 `.claude/skills`，不用等 30–60 秒缓存过期。

### Changed
- **皮肤配色大调（18 套全改底色）**：
  - **浅色 9 套不再「以白为主」**：大面积内容面（`--bg-2`）原来是 `lighten(bg, 0.5)`（底色和白对半混 → 实测亮度 88–95%、几乎纯白），改成 `lighten(bg, 0.26)`、面板 `0.3→0.14`，最大亮面压到 78–85%、保留纸张色；9 套底色各加深一档、酸性黄收一点刺眼。正文对比度复核仍全部过 WCAG AA。
  - **暗色 9 套去同质化**：原来 9 套底色相对亮度全挤在 0–1%（一样黑）、色相还扎堆（3 紫 / 3 暖 / 3 绿）。改成「深 / 炭 / 石板」三档深浅 + 三簇色相分家：像素光→品红、虚空→靛蓝、电光紫→电光紫；电路板→近黑绿、深海核→青、翡翠林→森林绿；工业轴→橄榄、霓虹夜→酒红、熔岩橙→橙。强调色 / 功能色 / 字体 / 终端 16 ANSI 一律不动。
- **上游基线 FanBox v2.3.1 → v2.3.3**：选择性合并上游更新——纳入上面的终端复制粘贴、skills 即时加载等核心修复；上游的「✕=隐藏窗口」我们 v2.7.0 已自有实现、上游 README 的 Windows 移植说明与我们无关，均略过。

### Fixed
- **终端 CJK 残影 / 字形图集损坏**（合并上游 FanBox #37 #45）：换肤 / 改字号 / 重排时 `clearTextureAtlas()` 强制重栅格化；换肤逻辑加 `try/catch` 容错，单个终端的 GPU 渲染故障不再连累其它终端或拖垮渲染进程。我们的彩虹标签 `tintTheme` 逻辑保留。
- **跟随绑到空闲终端时旁白栏不再「消失」**（合并上游 FanBox #30）：改显「等待 agent 写文件…」，不再让人以为跟随坏了。
- **微信链路去掉硬编码「花叔」**（合并上游 FanBox #48）：注入给 agent / 展示的运营者称谓改成通用「用户」，非原作者本人遥控时不再被告知「被花叔遥控」而跑错语境。

## [2.7.2] - 2026-06-28

### Changed
- **终端工具栏四个启动器换成各家官方品牌标的实心矢量**：Claude=Anthropic 官方星芒、Codex=OpenAI 官方花结、微信=官方双气泡，普通终端=实心圆角窗 + `>_`（mask 挖空，与三个品牌标同为实心、并排统一）。原来的图标是手画的、辨识度差（Codex 那朵云常被当成「Cloud Code」）。
- **工具栏分层**：四个启动器实心 + 高亮（`--text`），后面的次级动作（跟随眼 / 新标签 / 铺满 / 布局 / 录像 / 提示音 / 收起）统一线性 + 压淡（`--text-faint`），hover 各提一档；一眼分得清「启动谁」和「辅助操作」。

### Added
- **终端标签 hover 自定义光标**：斜箭头实心填当前皮肤强调色 + 白描边，跟 18 套皮肤一起换色（`themes-patch.js` 按 `--accent` 现拼 data URI 塞给 `--tab-cursor`）；不再是系统默认那只小手。

## [2.7.1] - 2026-06-28

### Changed
- **终端标签 / 面包屑配对色点改用黄金角分配色相**：原来 `hueOf` 按项目路径哈希取色相（`h*31+charCode`），但哈希被长路径的公共前缀主导（`/Users/rurutia/Documents/RuruCode/…`），几个不同项目会挤到相近色相——新开几个终端常常全是绿、分不清谁是谁（实测旧算法 Reverie=118、RuruAgent=137 都落在绿，差 19° 肉眼分不开）。改成按会话内出现顺序用黄金角（137.508°）铺色环，每个项目分到的色相尽量离已有的远（seq 0/1/2 → 25/163/300，橙/青/品红）；同一项目仍恒定一个色，标签↔面包屑色点配对不变。

### Fixed
- **面包屑「XXX 项目」徽章在顶栏被拖窄时压成两行、文字上下跳**：徽章是 flex 项却漏了 `white-space:nowrap` 和 `flex-shrink`，空间一紧就被挤压换行（「GIT 项目」断成两行像个圆圈，拖拽时还在一行/两行之间反复跳）。`soft-patch.css` 补上 `nowrap + flex-shrink:0`，徽章恒为一行硬药丸，挤压继续交给面包屑横向滚动（面包屑本就 `overflow-x:auto`）。

## [2.7.0] - 2026-06-27

### Added
- **皮肤升级成多强调色系统**：18 套皮肤从「单强调色推导」改为「中性 canvas + 3 个并列 accent（a1 主 / a2 次 / a3 跳）+ 一组状态色」。a1 管按钮/活动态/面包屑/光标，a2 管侧栏分区标题与链接（小字自动压成可读变体），a3 管项目角标实色填充。每套颜色更多、更跳，又靠共享底 + 角色分工保持协调；正文/强调色/按钮ink/徽章ink/终端 16 ANSI 全部通过 WCAG 对比度校验。

### Changed
- **浅色皮肤下终端标签底色改成淡染，不再像贴了块深补丁**：标签整块填的是项目色块（50% 亮度），在浅底上发闷、跟皮肤不搭。浅色皮肤改成高亮度、低不透明度的淡染，悬停 / 活动逐档加深做层级，左缘项目色脊保留当重点；深色皮肤不变。
- **去掉终端活动标签底部的项目色横线**：活动标签本来就有「左缘色脊 + 整块背景填充」两个指示器，底部再补一条下划线是第三个、多余 —— 删掉，标签更干净。

### Fixed
- **点红色 ✕ 改为隐藏窗口（macOS）**：原来点 ✕ 会销毁窗口并杀掉终端 PTY，再点 Dock 是新建窗口、数据清零，还得额外 ⌘Q 才真退（看着像「退两遍」）。现在 ✕ = 隐藏，点 Dock 原样唤回（终端、状态都在），⌘Q = 一遍真退。
- **浅色皮肤终端彩色输出可读性**：浅底上的绿/黄/青（尤其黄）对比过低、几乎糊进背景；压暗锚色 + 黄绿青重点压 + 修了在浅底隐形的 brightWhite，每套浅皮肤的终端彩字对比度都 ≥ 3.5。
- **Monaco 编辑器底色跟皮肤走**：编辑器背景原写死一块中性近黑 / 纸白，与皮肤一对比就突兀；改为按皮肤推导同色温底（`!important` 盖过 Monaco 主题），只动背景不碰语法配色。
- **Agent 用量板块的灰字看不清（浅色皮肤尤其明显）**：「窗口标签 / 重置倒计时 / token 单位 / 小注」这些弱字在浅底上对比度只有 ~2.3，基本读不出。把用量卡内的弱字两档整体压深（仅浅色皮肤，9 套浅皮肤全部过 WCAG 4.5）；深色皮肤的同款弱字本来也偏弱（~2.8），顺手轻提一档到 ~4.2。

## [2.6.1] - 2026-06-27

### Fixed
- **App 内「检查更新」指向错了仓库 → 桌面端永远收不到新版本**：更新检测的地址沿用了 fork 上游 `alchaincyf/fanbox` 的 releases，而 Rurutia 自己的版本发在 `Rurutia-Artemis/rurutia` —— 于是装着的 App 一直在上游查（那边最新还停在 FanBox 2.3.x），永远查不到 Rurutia 新版、不弹更新提示。改为指向 Rurutia 自己的发布仓库（`REL_PAGE` + GitHub API 两处）。**注意**：旧版里这个地址是写死错的，所以本次需手动下载安装一次；装上 v2.6.1 之后，往后的版本就能正常自动检测了。

## [2.6.0] - 2026-06-27

### Added
- **终端提示符可选（16 套主题 + 5 叠加修饰 · 独立于皮肤）**：侧栏皮肤下方新增「提示符」选择器，分两层——**整套主题**（药丸·摩卡 / 帕斯特尔 / 东京夜 / Gruvbox 彩虹 / Nord / Dracula / Rosé Pine / Everforest / Kanagawa / 拿铁浅色 等 10 套 powerline「全套」，加扁平彩字 / Jetpack 座舱 / Pure 简约 / 极简单行 / 两行 / 纯文本，共 16 套，选一个）+ **叠加修饰**（隐藏语言版本 / 纯文本符号·去图标 / 关时间 / 关命令耗时 / 去前导空行，可多选并行）。切换走自带的**零依赖 TOML 合并器**把「主题 + 勾选的修饰」深合并成一份 `active.toml`；`STARSHIP_CONFIG` 恒指它、starship 每次渲染都重读 —— **正在跑的终端按回车即变样，无需重开**。与皮肤完全解耦、独立持久化。仅 macOS + zsh。
- **「普通终端」快速启动按钮**：终端工具栏 Claude Code / Codex 旁新增一个普通终端按钮，一键在当前文件夹开一个干净 shell（不带 agent）。

### Fixed
- **换文件夹后启动 Claude Code / Codex，终端跑错目录、标签名不跟随**：原本「当前是空闲 shell 就就地启动」会复用停在旧目录的 shell，把 `claude`/`codex` 直接敲进去 —— agent 实际跑在旧文件夹（如换了目录仍显示 `Rurutia`）、标签名（跟随真实 cwd）也不变。改为复用前先 `cd` 到当前浏览目录，agent 在「现在这个文件夹」里跑，标签名随真实 cwd 自动刷新。新开标签本就在对的目录，不受影响。

## [2.5.0] - 2026-06-27

### Added
- **终端自带 Starship 提示符（开箱即用 · 可爱 Catppuccin powerline + ♥）**：内置 starship 二进制（已签名公证）+ Nerd Font 图标字，用户下载装完打开终端就是 powerline 药丸提示符（目录 / git 分支状态 / 语言版本 / ♥ 时间），无需自己装 starship、不用配 `~/.zshrc`。注入走 **ZDOTDIR**：自带的点文件先 `source` 用户真实 `~/.zprofile`/`~/.zshrc`（PATH、别名分毫不差，`claude`/`codex` 照样找得到），再在最后叠加 starship —— **只在本 App 终端生效、不碰用户任何 dotfile、卸载零残留**。仅 macOS + zsh 启用，其它 shell 自动跳过、终端照常。
- **Nerd Font 图标字内置**：`Symbols Nerd Font Mono` 随包，终端图标（powerline 箭头、git/语言图标、♥）不再依赖用户系统是否装过 Nerd Font。

### Changed
- **每套皮肤独立的终端配色**：终端背景 / 前景 / 光标 / 选区改为从当前皮肤推导（与面板同底、无缝衔接），ANSI 16 色按明暗复用基底保证可读 —— 终端背景真正跟着 18 套皮肤走，不再只有「暗 / 亮」两套。
- **项目类型徽章（面包屑「XXX 项目」）重设计**：原版 `var(--green)` 实心亮绿 + 硬黑字在柔和座舱里太「酸」、整体违和；改成跟随皮肤强调色的柔和药丸（`--accent-soft` 填充 + 细描边），与面包屑当前项同一套观感。

## [2.4.1] - 2026-06-26

### Added
- **发布流程自动化（签名 + 公证一条命令）**：新增 `npm run dist:release` —— 设 `APPLE_KEYCHAIN_PROFILE=rurutia-notary` 命中 electron-builder 的 keychain 公证路径，自动签名 + 公证 `.app`；再经 `afterAllArtifactBuild` 钩子（`build/notarize-dmg.js`）把 **DMG 容器本身**也提交公证 + staple。一条命令产出「下载直装」级 DMG（`.app` 与 `.dmg` 都带公证票、离线可校验）。公证密码只在 macOS 钥匙串里，不入环境变量 / 文件。普通 `npm run dist` 保持只签名不公证，本地迭代用、不被公证拖慢。

### Fixed
- **桌面 App 预览全屏后顶栏按钮全部点不动**：v2 改造把拖窗区从 `#topbar` 换成整个 `#app`（顶部 40px 留白带整条拖窗），但全屏预览（`#preview.is-max` = `position:fixed; inset:0` 铺满窗口）会盖住这条带，而 `-webkit-app-region:drag` 是 OS 级遮罩、不认 z-index，把压在它上面的预览工具栏（打开 / 关闭 / 复制 / 在编辑器打开…）按钮点击全吞掉了 —— 全屏后这些按钮全点不动，只能按 `Esc` 退出。补上 `.desktop.preview-maxed #app { -webkit-app-region:no-drag }`：全屏期间把 `#app` 整条拖窗区关掉（顶栏此刻被盖住、本就不需要拖窗），与原版对 `#topbar`/`.brand` 的同款修复对齐。**仅桌面 App 受影响，网页版无 app-region、不受影响。**

## [2.4.0] - 2026-06-26

### Rurutia 增强 v2（UI / 交互大改 · Developer ID 签名 + Apple 公证）

#### Added
- **深浅统一视觉规范**：底盘 `--bg` + 面板亮一档浮起，18 套皮肤深浅都成立；终端做成独立圆角卡 + 头部圆弧；主体拆成「文件卡 ┄ 状态行(空隙) ┄ 终端卡」。
- **用量分级徽章**：Claude `Max 5×/20×/Pro/Plus/Free`（从 `~/.claude.json` 的 `organizationRateLimitTier` 本地读）；Codex `Pro 20×`（`pro_lite`→`Pro 5×`，对齐 CodexBar 约定）。金 / 强调 / 灰三档质感徽章。
- **终端标签弹性拖拽换位**；**Agent 项目可拖入收藏 / 快速入口 + 列表内拖动排序**（localStorage 持久化）。
- **Skills 透视移到顶栏图标按钮**（再点切回文件、高亮态）；**Agent 用量做成侧栏底部醒目大卡**（默认展开）。
- **窗口顶部整条留白可拖拽** + 交通灯避让；**全局强制 Maple Mono**（中 / 英 / 日）；滚动条重做、变更收件箱卡片化、侧栏标题放大。
- **三语言 README**（中 / 英 / 日）+ 深浅主题截图（项目名已脱敏）；新增 `update-from-upstream.sh` 一键跟上游 rebase。

#### Fixed
- **Claude 用量「无数据」根因**：官方限额接口严格限流，改为 10 分钟缓存 + 限流时沿用上次缓存；`no-windows` 不重试，避免对限流接口多打一次。
- 终端拖拽时 `renderTabs` 重建竞态、拖拽尾随 click 监听泄漏、滚动条顶到圆角凸出、浅色主题四角脏黑块、`body *` 字体 force 对 Monaco codicon 的兜底。

## [2.3.1] - 2026-06-24

### Rurutia 首个签名 + 公证发布（fork 自 FanBox v2.3.1）
- 改名 **Rurutia** + 自定义图标；18 套「色所」配色皮肤 + 色卡选择器；全局 Maple Mono CN 字体。
- 快速入口 / Agent 项目可增删；用量面板增强（官方限额恒显 + ≥85% 警告条 + 桌面通知）；终端 10 个动作图标重绘为单色矢量；整体 UI 现代化。
- 自动端口顺延（4567 被占自动换下一对）；独立 bundle ID `com.rurutia.app`。
- **Developer ID 签名 + Apple 公证通过**：`Rurutia-2.3.1-arm64.dmg`，2026-06-24 三次提交均 `Accepted`（cert: RURU HOSHIZAWA · Z4AC57Z592）。

## [2.3.0] - 2026-06-18

### Added
- **微信 ClawBot 上下文管理：自动整理 + 新对话 + 用量进度条**：解决「越聊越贵越慢、上下文爆掉」。认清一个事实——对话上下文活在 claude/codex 自己的 session 里（每轮重放全部历史），FanBox 能控的只有「轮换 session」。于是 compact 和「新对话」共用一个机制，区别只在换之前留不留摘要续场：
  - **整理（compact）**：先静默跑一轮 memory-flush（让 agent 把要点用 `<memory>` ops 落盘 + 吐一段 ≤150 字进度摘要）→ 换新 session → 用摘要播种下一轮，接着聊不断档。
  - **新对话**：flush 一次保住记忆 → 硬清 session，之后纯靠长期记忆续，换话题用。
  - **自动闸门**：每个回合结束后，若单轮输入 token 超阈值（默认 120k，留足 claude 200k 窗口的垫）就**静默自动整理**，绝不打断当轮回答；聊天区留一条浅色「已自动整理上下文」。
  - **用量进度条**：面板顶栏一条轻量条（`38k / 120k`，≥80% 转红），一眼看出这条线多重、离自动整理多远。
  - 连续性由记忆层兜底：任何重置前都先 flush，重置永不丢重要信息。设计见 docs/09 §5。

## [2.2.1] - 2026-06-18

### Fixed
- **微信 ClawBot 跑一个回合期间收不到新消息（疑似「卡死」）**：根因是轮询循环把「拉消息」和「跑完整个 agent 回合」绑在一条线上（`await onWechatMsg`），而单个回合最长可达 10 分钟——这段时间 `getUpdates` 完全停摆，手机上再发消息也拉不进来，表现为微信一直「正在输入」、FanBox 却收不到最新消息。改为收发解耦：轮询线只把入站消息塞进**按会话分桶的队列**就立刻回去继续拉，agent 由独立 worker 串行消费（同一会话不并发、不跑乱上下文，不同会话各自并行）。轮询从此永不被 agent 阻塞，新消息随到随收；在忙时发的后续消息会排队等当前回合结束再处理。

## [2.2.0] - 2026-06-18

### Changed
- **微信 ClawBot 面板真实显示用户发来的图片**：之前面板里收到图片只显示「（图片）」占位文字，图片虽已下载落盘喂给 agent、却从不在会话气泡里展示，体验割裂。现在入站图片先下载解密拿到绝对路径，随消息一起带进气泡，前端用 `/api/raw` 直接读本机收件箱原图渲染缩略图，点击走全局 lightbox 放大；纯图无文字时不再多余地显示占位，文件附件 / 下载失败才退回文字占位。（历史消息不追溯显示，仅对新收到的图生效。）

## [2.1.0] - 2026-06-18

### Added
- **微信 ClawBot「离开不待机」一键开关**（macOS）：面板顶栏一个开关，打开后只要微信 ClawBot 还连着，合盖 / 息屏也不休眠——离开电脑也能用手机微信遥控本机的 Claude Code / Codex。复用现有 pmset 免密机制（首次需一次管理员密码）；断开微信或关掉开关自动恢复正常休眠，绝不让 Mac 一直不睡。
- **微信 ClawBot 连接状态主动监测 + 重连**：打开面板即主动探活（getconfig 轻量探测，不再只读「有没有存账号」），状态点分「检测中（琥珀脉冲）/ 已连（绿）/ 失效（红）」三态并配状态文案；连接失效（轮询发现 token 掉了、或探活失败）立刻翻红并弹出醒目重连横幅，一键弹二维码重扫，不再对着一个假绿点干瞪眼。
- **跨终端感知与控制**：微信 agent 现在能看到本机其他正在运行的终端——每轮把「终端花名册」（#编号、项目目录、前台进程、忙闲、最近输出尾巴）注入上下文，手机上随时问「电脑在跑啥 / 那个测试跑完没 / 卡哪了」直接答；还能通过 `<term>` 协议向指定终端发指令或按键（fire-and-forget，结果下一轮在状态里体现），离开电脑也能遥控正在干活的 agent。

### Fixed
- **打包版微信 ClawBot 驱动 claude/codex 报 `403 Request not allowed`**：根因是 App 从 Finder/Dock 启动只继承 macOS 阉割过的环境，没有代理 / 自定义 `ANTHROPIC_BASE_URL` 等变量，而 `zsh -lc` 又不读 `.zshrc`，导致 claude CLI 裸连被地域拦截。新增「终端环境复刻」（`electron/wechat/env.js`）：跑一次用户的交互式登录 shell 抓回完整环境（PATH / 代理 / BASE_URL / key 等），不假设任何特定代理方式；导入后仍无代理变量则读 macOS 系统代理（`scutil --proxy`）兜底，只补空缺不破坏无代理用户。启动时预热，第一条消息不卡。

### Changed
- **终端顶栏精简**：「文件跟随」去掉文字、只留图标；删除「定位文件」按钮（双击终端标签即可定位到其目录，功能重复）；终端录像键从 ▷（播放）换成胶片图标（语义对齐「录像/回放」），有录像的红点去掉粗糙的光晕环、收成干净小点。

## [1.13.0] - 2026-06-17

### Added
- **Markdown 支持 HEIC/HEIF 预览**：苹果 HEIC 图片在 markdown 里 `![](x.heic)` 现在能正常显示。`serveRaw` 遇 heic/heif 用 macOS `sips` 转码成 jpeg 并缓存（复用缩略图那套缓存/LRU，零新依赖），`/api/raw`、`/`、`/api/thumb` 三条路径透明覆盖。
- **文件区拖入 app 内 / 外部图片存盘**：微信收到的图、预览里的图（都是 `<img>`，拖动带的是 `text/uri-list` 而非系统文件）现在也能拖进文件管理区存到当前文件夹；并把 drop 区扩展到撑满整个文件夹高度——文件下方的空白也算文件夹内，不再「下半截拖不进」。

### Fixed
- **Markdown 编辑器静默改写 / 外部改动不同步**：富文本（Milkdown）是有损往返却被当成事实源写盘，导致渲染态 / 源码态 / 磁盘三份分裂，命令行或 agent 改盘后编辑器不重载、还会用旧内容覆盖。现在：磁盘原文为唯一事实源；只有「语义无损」（渲染结果一致）才允许富文本，否则锁源码并灰显切换按钮，杜绝静默改写；外部改盘时编辑器未脏自动重载、脏则保留你的改动靠保存时的冲突保护兜底。

## [1.12.0] - 2026-06-15

### Added
- **终端录像回放（黑匣子）**：常开录制每个终端会话，把 PTY 字节流旁路成 asciinema v2 `.cast`（异步写盘、失败静默自废，对终端零侵入）。回放用与 live 终端完全相同的 xterm 配置（主题随录像走、字体、unicode11、对比度），画面与当时逐像素一致。时间压缩「压等待不压输出」——把 agent 思考/装依赖的长静默封顶，连续吐字的流式节奏原样保留，2 小时会话可压到约 1 分钟。终端工具栏 ▶ 进入录像库（有录像点红点），列表显示时长，选中自动播放并跳过开头空白。导出走本机 ffmpeg 转 MP4 / GIF（检测不到 ffmpeg 优雅退回 WebM），画面就是回放本身。
- **微信 ClawBot 集成**：扫码把微信接到本机 OpenClaw（驱动 Claude Code / Codex），可读回对话内容；顶栏入口 + 连接状态。
- **合盖保持运行**：可让 FanBox 在合盖时不休眠，长任务 agent 继续跑。

### Fixed
- **截图直通车裂图**：Retina 全屏截图较大、写盘未完成时缩略图会拿到半截文件失败且不重试。改成等文件大小稳定再通知 + 卡片缩略图加载失败自动重试再优雅隐藏。

## [1.11.3] - 2026-06-15

### Fixed
- **终端里目录路径不可点击**：带斜杠的路径靠服务端 stat 验证才划线，而裸文件名（`xxx.md`）有扩展名白名单免验证直接划线、点开再按文件名搜索兜底。当终端 cwd 与打印的相对路径基准不一致时（agent 常这样），文件靠裸名兜底照样可点，目录没扩展名享受不到兜底，于是只显蓝不可点。现在结尾 `/` 的目录候选也享受同等兜底（验证过用精确路径，验证不过保留链接走 basename 搜索），并修了 `openTermPath` 对 `dir/` 取 basename 得空串导致搜不到的问题。
- **面包屑电脑图标偏高**：内联 SVG 默认 `vertical-align: baseline` 会顶到文字基线上方，改 `middle` 与同行文字居中（顺带修了「最近修改」的时钟图标）。
- **终端无法用鼠标选中文字**：claude/codex 等 TUI 开启鼠标上报后会吃掉拖拽，mac 上必须开 `macOptionClickForcesSelection` 才能强制选中，旧配置没开导致一点都选不了。现在按住 Option 拖拽即可选中复制（iTerm/VS Code 终端同款约定）。

## [1.11.2] - 2026-06-14

### Fixed
- **HTML 预览里本地图片裂图**：不同 coding agent 生成的 html 引图方式各异，`file://` 绝对 URL 和 `/Users` 裸绝对路径在 http 预览（沙箱 iframe）里都加载不了。现在预览会自动把图片引用兜底到 `/fs` 镜像端点——`file://` 主动改写（这类永远加载不了，改了只会帮忙），其余绝对路径仅在加载失败时才兜底重写（对本来能加载的相对/远程/data 引用零影响），真缺失的文件不会被假装修好。覆盖 `src` / `href` / `poster` 和内联 style 的 `background:url(file://…)`



### Added
- 内嵌终端字体优先使用系统已装的 **Nerd Font**（JetBrainsMono / MesloLGS / FiraCode / Hack / Symbols Nerd Font），让 Starship、powerline 等主题的图标和箭头正常显示，不再是方块 tofu

### Changed
- 内嵌终端改用 **login shell** 启动（`zsh -l`），会读取 `~/.zprofile` / `~/.zlogin` 里配的 PATH，解决「普通终端能找到 `claude`、FanBox 内嵌终端却找不到」
- 删除顶栏「← 后退 / ↑ 上一级」两个按钮：和面包屑导航功能重复，且收起侧栏时与 macOS 红绿灯重叠。后退 / 上一级保留 `⌘[` 和 `Backspace` 快捷键
- 收起侧栏时顶栏内容右移，给 macOS 红绿灯让位

### Fixed
- 加固：FanBox 访问自己的 localhost 后端时显式给 loopback 加代理旁路，避免 clash 强制系统代理、企业 PAC 等场景把本地请求拦成 502，导致整个界面白屏

## [1.11.0] - 2026-06-13

### Added
- 代码/文本编辑器改成「和 md 一致」：去掉「保存 / 完成」按钮，停笔自动落盘；顶部加**撤销 / 重做**两个图标（按撤销栈可用性灰显），「保存」状态实时显示**「xx 之前已保存」**（1 分钟内显秒、1 小时内显「分:秒」、再久显最后保存钟点）
- **双击终端顶栏空白处 = 终端铺满**：agent 交互窗口一键放到最大，挤掉文件区和预览
- 侧栏「收藏」「AGENT 项目」列表也会高亮**当前所在目录**（原先只有「快速入口」有指示），重渲后保持选中态

### Changed
- 折叠 / 展开侧栏时，腾出 / 收回的宽度在「文件区+预览」和终端之间**按比例分配**，不再全甩给左侧文件区（左右分栏）
- 默认设置调整（仅对全新 profile 生效）：默认皮肤「档案」、文件图标最小尺寸、终端首次**左右分栏**且文件区:终端 = **1:2**、文件区:预览 = **1:2**（拖过 resizer 后用记下的 px）
- 移除「文件区最小尺寸」自动挤压策略：导航不再强行改你的分栏比例。仅保留一个特例——终端铺满 / 全铺时主动导航会退出该状态，让文件区回来

### Fixed
- 预览全屏时左上角操作按钮（如 md「源码」）点不动：顶栏 `-webkit-app-region: drag` 是 OS 级遮罩、不认 z-index，会吞掉压在上面的预览按钮点击。全屏期间关掉顶栏拖拽区
- 预览全屏时隐藏 macOS 左上角红黄绿系统按钮（和右侧自家关闭图标太像，易误点），退出全屏恢复
- **图片全屏在窗口中居中**，整图缩放铺满可视区（约束 max-height 避免高图顶部被裁）
- **压缩包内中文文件名乱码**：不再依赖系统 unzip（会按自己的逻辑转码丢失编码），改为直接读 zip 中央目录，按 ZIP「通用位标记 bit 11」判断 UTF-8、否则按 GBK 解码

## [1.10.0] - 2026-06-13

### Added
- 终端里点 md/html 路径：定位打开后**直接全屏预览**，最贴合「点一下就想看清这文件长啥样」的意图；代码等其它类型仍退回常规分栏
- 文件区**双击文本/代码 = 全屏预览**（单击仍是分栏轻预览）；图片/视频等双击仍交系统默认 App 打开

### Changed
- **预览即编辑**：代码/纯文本一进预览区就是可编辑态（和 md 一致），不用再点编辑按钮；html（看的是渲染形态）、csv/tsv（表格视图）保持只读展示

### Fixed
- 文件跟随追到原子写临时文件（`foo.swift.tmp.<pid>.<hex>` 这类 `.tmp` 在中段的命名）后报 ENOENT、右上角挂着「跟随中 …tmp…」的怪状态：临时文件过滤从「`.tmp` 结尾」放宽到「`.tmp` 在结尾或后面还跟东西」，从源头拦掉

## [1.9.1] - 2026-06-13

### Added
- 文件跟随「过程旁白」（实时舞台阶段二/三）：预览主视图仍是 agent 产出的成品，底部加一行实时旁白说 agent 此刻在干嘛。从绑定终端 tab 的输出尾巴提炼动作（认 Claude Code 的 ⏺ Update/Bash/Read/Grep 工具行和 Web Search），翻成「写 X / 跑 Y / 搜 Z / 联网搜 W」；忙时「agent 正在 …」配脉冲点，闲下来「停在 X」配静止点。只认绑定 tab 的输出，不串别的终端
- 截图/文件可拖进文件区落盘：拖到空白处存进当前目录，拖到某文件夹图标上存进那个文件夹。macOS 截图浮窗缩略图（没磁盘路径）和 Finder 文件（有路径）都认；同名不覆盖（仿访达存成 foo 2.png）；落地有高亮提示

## [1.9.0] - 2026-06-13

### Added
- 预览全屏放大：顶栏一颗按钮把预览铺满整个窗口（盖住文件区/终端/侧边栏），Esc 或再点退出。md 全屏下依然所见即所得、可继续编辑；html/长文档放大后看着舒服
- HTML 预览支持完整交互：点击、键盘、localStorage、fetch、表单、SPA 全部能跑。靠「隔离预览源」做到安全——新增预览专用端口（主端口+1）只出文件、不挂 /api，预览页与 App 跨源，碰不到 App 本体也摘不掉 sandbox；可读范围收紧到主目录、挡掉点目录防敏感文件外泄

### Changed
- 文件跟随「钉死归属」：跟随死死锚定到你选的那个终端 tab，只盯这个 agent。归属从「路径前缀猜」升级成「路径 + agent 此刻在不在干活」双重判断，多 agent 多终端时别的 tab 的写入不再串进来。开跟随必须绑一个活动终端（没有就提示），不再退化成「全文件系统跟随」。UI 明确显示在跟哪个项目/agent：徽标带项目名、被盯的 tab 描边高亮 + 眼睛标记
- 文件跟随产物不再霸屏：agent 编译/打包出的二进制、压缩包、安装包（.app 内部、无扩展名可执行、.dylib/.zip/.dmg 等）不再被当文本跟随、显示「无法预览」，改成一张干净的产物卡片（图标 + 「agent 刚生成 · 大小」+ 在访达显示/打开）
- 底部少两条多余分割线：文件区和预览的底部信息栏不再各带横线（本就有背景色差自分隔），底部只剩终端那一条可拖的边界，不再有「拖哪条」的困惑

### Fixed
- 删掉 HTML 预览里那颗空着的没用小按钮（原是「适配宽度/实际大小」切换钮，因全局缺 .hidden 规则一直空露在外）；保留自动缩放适配预览框宽度的行为

## [1.8.3] - 2026-06-12

## [1.8.2] - 2026-06-12

### Fixed
- 文件跟随四处失控一并修掉：
  - agent 只是「读」文件也触发跟随和卡片闪烁——macOS FSEvents 连 atime/元数据更新都报。主进程转发事件前先 stat，mtime/ctime 都不新鲜（内容根本没动）的事件直接丢弃，读文件从此安静
  - 隐藏文件（.env、.git 配置等）和 sqlite 临时 sidecar（`-journal`/`-shm`/`-wal`/`.tmp`/`.lock`）也被跟随——用户什么都没的看。统一进噪声过滤，跟随/闪卡/变更收件箱全部不再理会
  - 跟随范围曾是「所有监听目录」：浏览到 ~/.cc-switch 时别的 App 写 db-journal 也被跟过去，噪声还在节流窗口里不断顶掉真正该看的 html/md。改为开启时绑定当前终端 tab，只跟它项目目录（cwd 随 agent 走）下的写入；切 tab 不跟别的 agent，绑定的 tab 关掉跟随自动停
  - 节流窗口内加看头优先级：html/md（写给人看的）> 代码 > 其它，低优先级写入不再顶掉排队中的高优先级目标
  - 关闭跟随时所有动作就地停：in-flight 的 html 双缓冲换页补上 follow 状态守卫，停掉后不再抢屏


## [1.8.2] - 2026-06-12

### Fixed
- 文件跟随四处失控一并修掉：
  - agent 只是「读」文件也触发跟随和卡片闪烁——macOS FSEvents 连 atime/元数据更新都报。主进程转发事件前先 stat，mtime/ctime 都不新鲜（内容根本没动）的事件直接丢弃，读文件从此安静
  - 隐藏文件（.env、.git 配置等）和 sqlite 临时 sidecar（`-journal`/`-shm`/`-wal`/`.tmp`/`.lock`）也被跟随——用户什么都没的看。统一进噪声过滤，跟随/闪卡/变更收件箱全部不再理会
  - 跟随范围曾是「所有监听目录」：浏览到 ~/.cc-switch 时别的 App 写 db-journal 也被跟过去，噪声还在节流窗口里不断顶掉真正该看的 html/md。改为开启时绑定当前终端 tab，只跟它项目目录（cwd 随 agent 走）下的写入；切 tab 不跟别的 agent，绑定的 tab 关掉跟随自动停
  - 节流窗口内加看头优先级：html/md（写给人看的）> 代码 > 其它，低优先级写入不再顶掉排队中的高优先级目标
  - 关闭跟随时所有动作就地停：in-flight 的 html 双缓冲换页补上 follow 状态守卫，停掉后不再抢屏


## [1.8.1] - 2026-06-12

### Changed
- 完成提醒升级：通知正文带 agent 最后回复的摘录（从终端缓冲区剥掉 TUI 框线/页脚捞正文），标题带项目名；点通知把 app 拉回前台并自动切到对应终端标签——多项目并行时落地就是要操作的那个环境

### Fixed
- 终端路径点不开：「看看效果：/tmp/x.png（…）、/tmp/y.png」这类全角标点紧贴路径的输出，两条路径全都不划线不可点。全角胶水标点（：；，。、？！）补进 token 切断字符集，前后都断；既有场景（.claude/x、中文相对路径、散文不误标、URL）行为不变
- 同根因补齐另外三层：裸文件名（「生成了 a.png、b.png」顿号列举只认得出第一个）、scrollback 回扫（路径后跟顿号漏配）、URL（「https://x.com/a、后续散文」粘连）——全角标点统一进各层边界字符集

## [1.8.0] - 2026-06-12

### Added
- 文件跟随：终端跟随退役，原位换成「文件跟随」。开着它跑 agent，agent 改哪个文件，文件区就跳到哪、预览实时跟着看——代码文件逐行高亮本次写入的行并平滑滚过去（hljs 按行切开渲染）；html 边写边出实时网页，新页面在隐藏 iframe 里加载完才换上（双缓冲，零白闪）；md 边写边渲染，尾部追加自动贴底滚动。一开就先跟上最近一次变更，不用干等下一笔；agent 在多文件间快写有节流，不会疯狂跳屏。任何手动浏览/编辑 = 接管，跟随立即自动停，想跟再点一下

### Removed
- 终端跟随（进文件夹终端自动 cd）：用处不大，按钮位让给文件跟随；「定位文件」保留

## [1.7.3] - 2026-06-12

### Changed
- 编辑图片时面板撑满整窗：原来编辑器挤在小预览栏里，画布太小没法精细标注。进入编辑即全屏（画布居中放大到视口最大），保存/关闭/切文件自动还原。纯 CSS `:has()` 实现——感知编辑器 DOM 是否在面板里，不靠 JS 维护全屏状态，4 个退出路径都不可能漏还原

### Fixed
- 打开文件就误亮「改」徽标：macOS 打开文件时 LaunchServices 会写 `com.apple.lastuseddate#PS` 扩展属性，FSEvents 据此连发事件，内容没动也被当成 agent 改动。改为自己发起的打开记下路径，3 秒内该文件的变更事件按噪声整条丢弃（不点卡、不进变更收件箱、不触发刷新）；agent 真改/移动文件的徽标不受影响


## [1.7.2] - 2026-06-11

### Changed
- AI 整理推倒重做（v2）：废弃后台 headless 跑 `claude -p` 的弹窗式三段流程（报错黑盒、干等一两分钟、一锤子提案不可用），改为一键在内嵌终端拉起交互式 Claude Code / Codex 对话整理。FanBox 负责备料——把整理偏好、最近整理历史、工作约定写成 `~/.fanbox/organize-brief.md`，agent 读完先摊方案、用户在终端对话里确认调整后才动手；每批移动照旧写 `~/.fanbox/organize-log/` 回滚日志（撤销在对话里说一声即可）；删除提议须逐条点头、进废纸篓不直接 rm；收尾把这次学到的用户偏好沉淀进 `~/.fanbox/organize-prefs.md`，越用越懂你

### Fixed
- 终端按钮在激活态「隐身」：两条 CSS 规则打架，激活时文字/图标被改回强调色、和强调色背景同色——删掉遗留的覆盖规则
- 侧栏滚动时灵时卡：整栏被设为窗口拖拽区（#6），而 Electron 拖拽区收不到滚轮事件，鼠标停在标题/缝隙上就滚不动——回退侧栏整体拖窗，拖窗用品牌区和顶栏


## [1.7.1] - 2026-06-11

### Added
- 终端里的 http(s) 链接可直接点击，在系统浏览器打开

### Fixed
- 终端路径识别改为「宽进严出」：凡含斜杠的 token 都收为候选（`.claude/skills/…` 点开头路径、`写作/01-xx` 中文相对路径从此都能点），先 stat 验证、验证得到才划下划线（含行尾空格扩展，结果按会话目录缓存）——中文散文里的「分发/产品演示——……」不再整句被当成可点路径；验证接口不可用时宁可不划线
- 完成提醒误报：agent 只是阶段性收工、底部还挂着后台任务（「1 shell, 1 monitor still running」等）时，不再弹「任务完成」通知和提示音——圆点照常变空闲，等后台任务跑完、agent 真正收工再报

## [1.7.0] - 2026-06-11

### Added
- AI 整理（#2）：「AI 提案、人批准、FanBox 执行」三段式——AI 只看元数据出建议（不读内容不碰文件），每条建议带理由逐条勾选，执行写回滚日志、一键整体撤销（日志留 30 天）。引擎可选 Claude Code / Codex，整理策略提示词可编辑可恢复默认。入口：空白处/文件夹右键「AI 整理…」
- 英文支持（i18n）：默认跟随系统语言，侧栏左下角可手动切换并记住。集中式翻译层（326 词条 + 73 条插值规则），界面、菜单、系统对话框全覆盖；文件名、预览正文、编辑器、终端等用户内容不受影响
- 空白处右键弹「新建文件夹 / 新建文件」菜单（#7），双击入口保留
- 按住侧栏空白可拖动窗口（#6）

### Changed
- 产品正式定名 **FanBox**，去掉「翻箱」中文名：应用名、菜单、品牌区、dmg 文件名统一（安装新版后请手动删除旧的「翻箱 FanBox.app」）
- README 改为中英双语单文件
- 自动更新检测提频：周期 12 小时 → 2 小时，窗口重新聚焦补查一次（30 分钟节流）——发版当天老版本不再要等满周期才知道有新版

## [1.6.0] - 2026-06-11

### Added
- 截图直通车：监听系统截屏落盘（识别截屏命名习惯），新截图右下角浮出直通卡——「→ 终端」喂给 agent、「收进素材」移入当前文件夹 素材/、「标注」圈重点再发，不理它 45 秒自己走。从此告别 NSIRD 临时路径拖拽
- 发版向导：node 项目的状态条出现「发版」——版本号（预填 patch+1）、发布说明（预填 CHANGELOG 的 Unreleased 段）、打包/推送/GitHub Release 可勾选，确认后整条命令序列在内嵌终端开跑，每步可见可拦
- 项目记忆：状态条「项目记忆」打开当前文件夹的 agent 会话考古——历史会话列表（首条消息当标题）、每次会话改过的文件（点击直达）、触发过的 skill，「▶ 续上」一键在内嵌终端 claude --resume / codex resume 接上当时的上下文。数据源本地会话日志，按文件 (size, mtime) 缓存增量解析
- 底部状态条：文件区底缘小字常驻当前文件夹基础信息（N 项 · 文件夹/文件数 · 文件合计大小）
- 磁盘占用透视：状态条「占用透视」/ 右键文件夹进入，du 口径的真实占用条形榜，目录可下钻、可回上级，解「电脑空间又满了」
- 压缩包预览（#3）：zip/jar/tar/tgz/gz 在预览面板列出内容清单和单项大小，全用系统自带工具（unzip/bsdtar/gzip），保持零依赖
- 透明图棋盘格（#1）：缩略图、预览大图、灯箱给图片垫棋盘格底，白色/透明 PNG 一眼可辨

### Fixed
- 透明 PNG/GIF/WebP 缩略图被拍成白底：sips 一律出 jpeg 丢了透明通道，带透明可能的格式改出 png


## [1.5.2] - 2026-06-11

### Changed
- 顶栏防挤压整修：右侧操作区不再参与收缩（挤压压力交给可滚动的面包屑），图标在任何宽度下不变形
- 「变更」「最近」改为纯图标按钮（悬停有提示）；「变更」数字徽标改成小圆点，有变更才亮，不再频繁跳数
- 「终端」按钮强调色突出：强调色描边 + 浅底，悬停/激活整体填充
- 顶栏窄时分级隐藏低频控件（缩略图大小 → 隐藏文件开关 → 视图切换/「最近」→ 排序），用 ResizeObserver 观测顶栏自身宽度而非视口——侧栏占宽且可折叠，按视口算会把最重要的终端按钮先裁掉

## [1.5.1] - 2026-06-11

### Fixed
- 打包版 Claude Code 官方限额（5h 窗口 / 周配额）一直不显示：App 从 Finder/Dock 启动时没有 shell 的代理环境变量，内置 curl 直连 api.anthropic.com 被 403 地域拦截。现在没有代理变量时自动读 macOS 系统代理（`scutil --proxy`，Clash 等都会写入）兜底
- macOS 移入废纸篓必败（含 Skills 面板的卸载）：AppleScript 的 `POSIX file` 缺 `as alias` 强转，Finder 报 -1728

### Changed
- Agent 用量面板的 Claude Code 区块：限额进度条优先展示，三格本地 token 用量（近5h / 今日 / 本周）完整保留在下方，不再压缩成一行小字

## [1.5.0] - 2026-06-11

### Added
- 「Skills 透视」面板（侧栏左下角入口）：本机所有 agent skills 的统一视图
  - 聚合五类来源：`~/.claude/skills`、最近 agent 项目的 `.claude/skills`、Claude 插件、`~/.codex/skills`、`~/.agents/skills`，跨端同名 skill 标「N 处副本」
  - 触发统计：解析 Claude Code 会话日志（模型自动触发 + 用户手动 / 调用）和 Codex rollout（按会话去重），45 天触发次数 + 最后触发时间，一眼分出活跃和吃灰
  - 健康检查：description 超 1536 字符截断线（后段触发词模型看不见）、缺 frontmatter、缺 SKILL.md、zip 等残留物，红黄绿标注 + 「仅看问题」过滤
  - Context 预算条：全局常驻的描述总量 vs 预算估算线，超限红色警示（超出部分会被静默丢弃）
  - 启停开关：停用 = 移入 `_disabled/` 子目录（立即对模型不可见、不删文件、可逆）。不用官方 skillOverrides——该配置在用户级有已知失效 bug（claude-code#50631）。软链接型 skill 先解析绝对目标再迁移，避免相对链接断链
  - ▶ 终端调用 + 拖拽：skill 行拖进内嵌终端（或点详情按钮），按会话里跑的 agent 自动注入 `/skill-name`（Claude Code）或 `$skill-name`（Codex）
  - 卸载：移到系统废纸篓，随时可恢复

## [1.4.3] - 2026-06-11

### Added
- 侧栏目录树：快速入口、收藏的文件夹项带展开箭头，逐级懒加载子文件夹（只列文件夹），点行仍是跳转
- 侧栏「Agent 项目」：替代原「最近打开」，自动扫 Claude Code / Codex 本机会话日志，列出最近 30 天被 coding agent 处理过的项目文件夹（agent 圆点 + 活跃时间），点击直达项目目录

### Changed
- 网格缩略图三档整体缩小约 30%（列宽 96/140/190 → 68/98/132），同屏可见文件数翻倍
- Agent 用量面板的 Claude Code 改为官方限额口径：从 Keychain 读 Claude Code 的 OAuth 凭证查官方接口，显示和 `/usage` 同源的「5h 窗口 / 周配额」百分比与重置时间；本地 token 统计降为辅注，拿不到官方数据时自动回退。该请求是翻箱唯一出网请求，只发往 api.anthropic.com（经系统 curl，自动继承代理环境）

### Fixed
- Agent 用量面板展开后收不起来：样式表缺通用 `.hidden` 规则，折叠类不生效
- Codex 用量严重失真：读的是上次会话的限额快照，窗口重置后旧百分比仍在显示（如 21 小时前的 57%）——快照过了重置时间即归零并标注「窗口已重置」
- 桌面 app 暖色皮肤丢失文件区纸纹纹理：浏览器版纹理从 body 透出，桌面版被 vibrancy 配套的不透明底色盖住，单独补回

## [1.4.2] - 2026-06-11

### Added
- 侧栏底部「Agent 用量」面板：读本机会话日志展示 Claude Code（近5h/今日/本周 token）与 Codex（5h 窗口、周配额官方百分比 + 重置时间），不用开着两个 CLI 也能看；可开合、开着时每分钟自动刷新
- 菜单「检查更新…」：手动检查新版本，结果用系统对话框反馈（发现新版/已最新/检查失败）
- 文件区空白处双击弹出「新建文件夹/新建文件」菜单

### Changed
- Claude Code / Codex 启动按钮行为统一：当前标签是空闲 shell 就地启动，正跑着任务（含已开启的 agent）则新开标签，不再往运行中的程序里打字
- 移除文件区顶部工具行（快速筛选框、文件计数、新建按钮），文件区上下空间更紧凑
- 滚动条改为细圆角胶囊样式（带透明边距），hover 色跟随主题，不再是写死的深色粗条

### Fixed
- 终端标签的中文目录名显示为 `\xe8\xa7\x86` 转义乱码、双击定位失效：GUI 启动的 app 没有 UTF-8 locale，lsof 把中文按字节转义——调 lsof 时显式给 locale 并加解码兜底
- 自动更新检测失效：GitHub API 在代理共享出口 IP 下经常被限流（403）后静默放弃 12 小时——失败时退回解析 releases 页重定向（不占 API 配额），并 10 分钟后重试；渲染层启动时主动补拉一次，不再因为加载慢错过主进程的推送

## [1.4.1] - 2026-06-11

### Fixed
- 终端六处体验问题：启动列宽、通知误报、输入法双写、标签识别、滚动、定位
- 安全：POST 写接口加 Origin 校验，挡 CSRF

## [1.4.0] - 2026-06-10

### Added
- 应用内更新提醒：启动时查 GitHub Releases，有新版本右下角弹胶囊引导下载

## [1.3.5] - 2026-06-10

### Fixed
- 终端文件路径识别全面重做：带空格/中文/折行路径可点 + 误报治理

## [1.3.4] - 2026-06-10

### Added
- 图片拖进 md 编辑器带原文件路径；本地路径图片走 /fs/ 兜底显示

## [1.3.3] - 2026-06-10

### Added
- md 预览即编辑：打开就是所见即所得，停笔 0.8s 自动保存

## [1.3.2] - 2026-06-10

### Fixed
- HTML 预览改走 /fs/ 路径镜像端点，本地相对引用图片/CSS 不再裂

## [1.3.1] - 2026-06-10

### Changed
- 编辑器/预览/终端累积迭代 + vendor 资产

## [1.1.0] - 2026-06-10

### Added
- 桌面驾驶舱：Electron 壳 + 内嵌终端（xterm.js + node-pty）+ 编辑器 + 图片编辑 + 布局重构

## [1.0.0] - 2026-06-09

### Added
- 首个版本：零依赖本地文件指挥中心（浏览/搜索/预览/轻改 + 缩略图 + 收藏）
